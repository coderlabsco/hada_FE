import type { ThemeConfig } from 'antd'

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: 'black'
  },
  components: {
    Form: {
      labelFontSize: 14,
      labelColor: '#444444'
    }
  }
}

export default theme
